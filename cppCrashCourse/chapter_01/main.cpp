#include <cstdio>

// First program - hello, world.
int main()
{
    printf("Hello, world!");

    // if statements
    int x = 0;
    if (x > 0)
        printf("Positive");
    else if (x < 0)
        printf("Negative");
    else
        printf("Zero");

    return 0;
}
